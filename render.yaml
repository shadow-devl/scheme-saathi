services:
  - type: web
    name: avenik-backend
    env: node
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: avenik-db
          property: connectionString

databases:
  - name: avenik-db
    plan: free
